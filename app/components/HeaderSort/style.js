import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerContainer: {
    height: 100,
    marginTop: 40,
    marginHorizontal: 10,
    borderColor: "#080201",
    borderWidth: 1,
  },
});

export default styles;
