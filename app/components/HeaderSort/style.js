import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerContainer: {
    height: 100,
    borderWidth: 1,
    borderColor: "black",
    marginTop: 40,
    marginHorizontal: 10,
  },
});

export default styles;
